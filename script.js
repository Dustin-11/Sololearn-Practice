/*This is a workspace to peform Code Coach practice exercises offered through the Sololearn app*/
//For some reason they do not allow for JavaScript solutions

function calc(houses) {
if (houses >= 3) {
    let perc = (2/houses) * 100; 
    Math.round(perc)
    console.log(perc);
}
}

calc(10);
calc(20);
calc(4);